import pandas as pd
#to read a csv file
df=pd.read_csv("data.csv")
print(df)#it reads the first 5 rows of the dataset and last 5 rows of the dataset and truacates the middle part of the datasetccsv file is a comma separated values file which is used to store tabular data in plain text format
#print(df.head())#it reads the first 5 rows of the dataset
#print(df.tail())#it reads the last 5 rows of the dataset
