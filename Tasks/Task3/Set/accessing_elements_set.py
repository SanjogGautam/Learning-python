#accessing the elements in a set
#in set as it is unordered we can't access the elements in a list by referring to an index or a key
x={1,2,3,4}
#we can access it through for loops using in
for i in x:
    print(i)
